//
// Created by misterm on 13/09/19.
//

#ifndef MYTEST_ENUM_H
#define MYTEST_ENUM_H

enum STATUS {
    ON = 1,
    OFF = 0,
};

enum MUTE_STATUS {
    NOMUTED = 0,
    MUTED = 1,
};

enum STEP_STATUS {
    NOSTEP = 0,
    STEP = 1,
};

enum SOLO_STATUS {
    NOSOLO = 0,
    SOLO = 1,
};

#endif //MYTEST_ENUM_H
